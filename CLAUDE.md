# idss-mesa.github.io

Follow the repository agent guide in [AGENTS.md](AGENTS.md). It covers the
OKF v0.2 bundle under `okf/`, the page-to-twin pairing, the files generated by
`scripts/gen_agent_surface.py`, the validation commands, and the rule that
generators never add `verified:`.
